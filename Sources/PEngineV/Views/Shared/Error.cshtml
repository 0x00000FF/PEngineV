@model ErrorViewModel
@inject Microsoft.Extensions.Localization.IStringLocalizer<PEngineV.SharedResource> L
@{
    ViewData["Title"] = L["Error_Title"];
}

<div class="pe-center-panel">
    <div class="pe-panel">
        <div class="pe-panel-header">
            <i class="fa-solid fa-circle-exclamation pe-panel-icon"></i>
            @L["Error_Title"]
        </div>
        <div class="pe-panel-body">
            <div class="pe-error-code">!</div>
            <h2>@L["Error_Message"]</h2>

            @if (Model.ShowRequestId)
            {
                <p class="pe-mt-2 pe-text-muted">
                    @L["Error_RequestId"]: <code>@Model.RequestId</code>
                </p>
            }
        </div>
    </div>
</div>
