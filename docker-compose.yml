services:
  penginev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=penginev;Username=penginev;Password=penginev
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - penginev-network

  nginx:
    build:
      context: ./Dependencies/nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Deploy/pengine-v.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - penginev
    networks:
      - penginev-network

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: penginev
      POSTGRES_USER: penginev
      POSTGRES_PASSWORD: penginev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U penginev"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - penginev-network

volumes:
  postgres-data:

networks:
  penginev-network:
    driver: bridge
